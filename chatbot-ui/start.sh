#!/bin/bash

# Script to start the chatbot UI with npm start
# Navigate to the script directory
cd "$(dirname "$0")"

echo "Starting chatbot UI..."
echo "Running npm start in: $(pwd)"

# Run npm start
npm start
